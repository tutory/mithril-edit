{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "watchify index.js -o app.js --debug",
    "build": "browserify index.js -o app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "domready": "^1.0.8",
    "mithril": "^0.2.4"
  }
}
